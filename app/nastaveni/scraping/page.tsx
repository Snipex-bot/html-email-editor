export const dynamic = "force-dynamic";

import ScrapingAdmin from "@/components/ScrapingAdmin";

export const metadata = { title: "Scraping produktů — Admin" };

export default function ScrapingPage() {
  return <ScrapingAdmin />;
}
